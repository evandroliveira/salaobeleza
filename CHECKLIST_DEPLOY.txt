✅ CHECKLIST: TUDO PRONTO PARA DEPLOY

═══════════════════════════════════════════════════════════════════════════════

📋 O QUE FOI PREPARADO:

✅ Procfile
   └─ Criado: web: gunicorn run:app

✅ requirements.txt
   └─ Atualizado com:
      • flask==3.0.0
      • flask-sqlalchemy==3.1.1
      • werkzeug==3.0.1
      • gunicorn==21.2.0 (NOVO)
      • psycopg2-binary==2.9.9 (NOVO)

✅ .gitignore
   └─ Criado com:
      • __pycache__/
      • *.py[cod]
      • *.db (SQLite excluído)
      • .env (segredos não enviados)

✅ run.py
   └─ Atualizado:
      • Suporta DATABASE_URL (PostgreSQL)
      • Suporta PORT dinâmica
      • DEBUG automático por FLASK_ENV
      • SECRET_KEY via variável ambiente

═══════════════════════════════════════════════════════════════════════════════

🚀 PRÓXIMAS AÇÕES (VOCÊ DEVE FAZER):

PASSO 1: GIT & GITHUB
[ ] 1. Instalar Git: https://git-scm.com/download/win
[ ] 2. Criar conta GitHub: https://github.com/signup
[ ] 3. No PowerShell (em c:\projetos\salao):
         git init
         git add .
         git commit -m "Initial commit"
[ ] 4. Criar repositório: https://github.com/new
[ ] 5. Conectar:
         git remote add origin SEU_LINK
         git branch -M main
         git push -u origin main

PASSO 2: RENDER
[ ] 1. Criar conta: https://dashboard.render.com
[ ] 2. Clique: New + → Web Service
[ ] 3. Selecione seu repositório GitHub
[ ] 4. Preencha:
      Name: salao-agendamento
      Environment: Python 3
      Build Command: pip install -r requirements.txt
      Start Command: gunicorn run:app
[ ] 5. Clique: Add Environment Variable
      FLASK_ENV = production
      SECRET_KEY = (gere com: python -c "import secrets; print(secrets.token_hex(32))")
[ ] 6. Clique: Deploy

PASSO 3: BANCO DE DADOS
[ ] 1. No Render: New + → PostgreSQL
[ ] 2. Preencha:
      Name: salao-db
      Database: salao
      User: salao_user
[ ] 3. Clique: Create Database
[ ] 4. Copie: Internal Database URL
[ ] 5. No Web Service, adicione:
      DATABASE_URL = (URL copiada)
[ ] 6. Clique: Deploy novamente

PASSO 4: TESTE
[ ] 1. Copie URL do Render (parecido com: https://salao-agendamento.onrender.com)
[ ] 2. Abra no navegador
[ ] 3. Login:
      Email: admin@salao.com
      Senha: admin123
[ ] 4. Teste:
      - Dashboard ✅
      - Agendar horário ✅
      - Admin panel ✅
[ ] 5. 🎉 PARABÉNS! APP ONLINE!

═══════════════════════════════════════════════════════════════════════════════

📁 ARQUIVOS CRIADOS PARA HELP:

├── Procfile                ← Deploy
├── requirements.txt        ← Dependências atualizadas
├── .gitignore             ← Segurança Git
├── run.py                 ← Atualizado para produção
├── DEPLOY.md              ← Guia completo (60+ linhas)
├── DEPLOY_RAPIDO.md       ← Versão rápida
├── COMO_SUBIR_ONLINE.txt  ← Este documento
├── TROUBLESHOOTING.md     ← Resolução de problemas
└── README.md              ← Visão geral do projeto

═══════════════════════════════════════════════════════════════════════════════

⚙️  CONFIGURAÇÕES IMPORTANTES:

1. SECRET_KEY
   - Gerada automaticamente
   - Não repita a mesma chave em diferentes ambientes
   - Gerar nova: python -c "import secrets; print(secrets.token_hex(32))"

2. FLASK_ENV
   - production → DEBUG=False (seguro)
   - development → DEBUG=True (desenvolvimento)

3. DATABASE_URL
   - Render PostgreSQL fornece automaticamente
   - Formato: postgresql://usuario:senha@host:porta/banco

4. PORT
   - Render fornece via variável PORT
   - run.py detecta automaticamente
   - Local: 5000 (padrão)

═══════════════════════════════════════════════════════════════════════════════

🔐 SEGURANÇA - CHECKLIST:

[ ] SECRET_KEY alterada (não use valor padrão)
[ ] DEBUG=False em produção (automático com FLASK_ENV)
[ ] HTTPS ativado (automático no Render)
[ ] Banco PostgreSQL (não SQLite)
[ ] Variáveis sensíveis em Environment Variables (não no código)
[ ] .gitignore evita envio de *.db
[ ] Senha admin será alterada (você decide quando)

═══════════════════════════════════════════════════════════════════════════════

💡 DICAS FINAIS:

1. Primeira vez é a mais difícil. Tenha paciência!

2. Leia os logs do Render se algo der errado:
   Dashboard → Logs → procure "ERROR"

3. Se ficar lento, upgrade Render para plano pago ($5/mês)

4. Para atualizar app:
   git add .
   git commit -m "mensagem"
   git push
   (Render deploy automaticamente!)

5. Guarde sua DATABASE_URL com segurança
   (não compartilhe, é sua conexão com o banco!)

6. Faça backup regularmente dos dados

7. Teste sempre em desenvolvimento antes de fazer push

═══════════════════════════════════════════════════════════════════════════════

🆘 SE ALGO DER ERRADO:

1. Leia TROUBLESHOOTING.md (neste diretório)
2. Verifique logs do Render
3. Execute git status (para ver estado)
4. Releia este documento
5. Procure erro no Google
6. Consulte documentação oficial:
   - Render: https://render.com/docs
   - Flask: https://flask.palletsprojects.com/
   - PostgreSQL: https://www.postgresql.org/docs/

═══════════════════════════════════════════════════════════════════════════════

📱 URLS IMPORTANTES:

GitHub:     https://github.com
Render:     https://dashboard.render.com
GitScript:  https://git-scm.com/download/win
Flask:      https://flask.palletsprojects.com/
PostgreSQL: https://www.postgresql.org/docs/

═══════════════════════════════════════════════════════════════════════════════

✨ RESULTADO FINAL:

Depois de completar todos os passos, você terá:

✅ App rodando online (24/7)
✅ Banco de dados em PostgreSQL
✅ HTTPS automático
✅ Deploy automático (via GitHub)
✅ Logs em tempo real
✅ Fácil de atualizar
✅ Escalável se precisar
✅ Profissional e seguro

URL final: https://seu-app.onrender.com

═══════════════════════════════════════════════════════════════════════════════

🎯 RESUMO:

Arquivos: ✅ TODOS PRONTOS
Código: ✅ PRONTO PARA PRODUÇÃO
Documentação: ✅ COMPLETA (4 arquivos)

Você só precisa fazer o PASSO 1-4 acima!

═══════════════════════════════════════════════════════════════════════════════

BOA SORTE! 🚀 SEU APP ESTARÁ ONLINE EM 20 MINUTOS! 🎉
